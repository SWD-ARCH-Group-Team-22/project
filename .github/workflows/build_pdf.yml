name: Build PDFs from Markdown

on:
  push:
    branches:
      - '**'  
    paths:
      - "doc/deliverables/md_reports/**/*.md"
      - ".github/workflows/build-pdf.yml"
  workflow_dispatch:

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install System Dependencies (Pandoc, LaTeX, Graphviz, Java, SVG Support)
        run: |
          sudo apt-get update
          # Aggiunto inkscape per garantire la massima compatibilità di conversione SVG -> PDF in Pandoc
          sudo apt-get install -y --no-install-recommends \
            pandoc \
            texlive-latex-base \
            texlive-fonts-recommended \
            texlive-latex-recommended \
            texlive-latex-extra \
            lmodern \
            librsvg2-bin \
            inkscape \
            graphviz \
            default-jre

      - name: Install Latest PlantUML
        run: |
          # Scarica una release stabile e recente di PlantUML
          wget https://github.com/plantuml/plantuml/releases/download/v1.2024.4/plantuml-1.2024.4.jar -O /tmp/plantuml.jar
          # Crea l'eseguibile globale "plantuml" affinché pandoc-plantuml-filter lo trovi
          echo '#!/bin/bash' | sudo tee /usr/local/bin/plantuml
          echo 'java -jar /tmp/plantuml.jar "$@"' | sudo tee -a /usr/local/bin/plantuml
          sudo chmod +x /usr/local/bin/plantuml

      - name: Install Pandoc PlantUML Filter
        run: pipx install pandoc-plantuml-filter

      - name: Create output directories
        run: |
          mkdir -p doc/deliverables
          mkdir -p res

      - name: Convert deliverables Markdown files to PDF
        run: |
          shopt -s nullglob globstar
          
          # Salva il percorso assoluto della root del repository
          REPO_ROOT="$PWD"
          
          for file in doc/deliverables/md_reports/**/*.md; do
            if [[ "$file" == *"svg.md" ]]; then
              echo "Skipping $file (SVG markdown file)..."
              continue
            fi
            
            # Estrae il nome del file e la cartella in cui si trova
            name=$(basename "$file" .md)
            file_dir=$(dirname "$file")
            
            echo "Converting $file to doc/deliverables/${name}.pdf..."
            
            # 1. ENTRA nella cartella specifica del file markdown
            cd "$file_dir"
            
            # 2. Esegue pandoc dal punto di vista corretto per i percorsi ../../../
            # L'output viene salvato usando il percorso assoluto salvato all'inizio
            pandoc --filter pandoc-plantuml \
                  -V geometry:margin=2cm \
                  "${name}.md" -o "$REPO_ROOT/doc/deliverables/${name}.pdf"
                  
            # 3. TORNA alla radice del repository per il prossimo ciclo
            cd "$REPO_ROOT"
          done
      - name: Commit and push generated PDFs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          git add doc/deliverables/*.pdf || true
          
          # Procedi solo se ci sono effettivamente dei cambiamenti
          if ! git diff --cached --quiet; then
            git commit -m "chore: update generated PDFs"
            
            # Fetch e pull senza rebase (merge) per integrare eventuali push di altre pipeline
            git pull --no-rebase origin ${{ github.ref_name }}
            
            git push
          else
            echo "No changes to commit."
          fi

      - name: Upload PDFs
        uses: actions/upload-artifact@v4
        with:
          name: generated-pdfs
          path: doc/deliverables/*.pdf
          if-no-files-found: warn